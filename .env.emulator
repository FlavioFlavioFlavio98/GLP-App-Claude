# Attiva la modalità test: la web app si connette agli emulatori Firebase locali
# invece che al progetto reale. Usato solo da `npm run dev:emulator` (Vite legge
# questo file automaticamente in modalità "emulator"). Nessun segreto qui dentro,
# va bene tenerlo nel repository.
VITE_USE_EMULATOR=true
