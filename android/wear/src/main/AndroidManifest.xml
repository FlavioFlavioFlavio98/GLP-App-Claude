<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.DeviceDefault">

        <uses-library
            android:name="com.google.android.wearable"
            android:required="true" />

        <!-- App standalone: gira anche senza telefono associato vicino, dato
             che fa il proprio login Google diretto (vedi LoginScreen.kt). -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity
            android:name="com.flavio.glp.wear.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity="">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Tile "punti di oggi" — raggiungibile scorrendo da destra sul
             quadrante senza aprire l'app. -->
        <service
            android:name="com.flavio.glp.wear.PointsTileService"
            android:exported="true"
            android:label="Punti oggi"
            android:permission="com.google.android.wearable.permission.BIND_TILE_PROVIDER">
            <intent-filter>
                <action android:name="androidx.wear.tiles.action.BIND_TILE_PROVIDER" />
            </intent-filter>
            <meta-data
                android:name="androidx.wear.tiles.PREVIEW"
                android:resource="@drawable/tile_preview_points" />
        </service>

        <!-- Tile "task oggi" — tocco apre l'app direttamente sulla pagina
             Task, senza dover scorrere manualmente le pagine. -->
        <service
            android:name="com.flavio.glp.wear.TasksTileService"
            android:exported="true"
            android:label="Task oggi"
            android:permission="com.google.android.wearable.permission.BIND_TILE_PROVIDER">
            <intent-filter>
                <action android:name="androidx.wear.tiles.action.BIND_TILE_PROVIDER" />
            </intent-filter>
            <meta-data
                android:name="androidx.wear.tiles.PREVIEW"
                android:resource="@drawable/tile_preview_tasks" />
        </service>

        <!-- Tile "Nuova task" — tocco apre subito la dettatura vocale, senza
             passare dalla lista task (sostituisce la App Shortcut, non
             supportata dal launcher di Wear OS). -->
        <service
            android:name="com.flavio.glp.wear.AddTaskTileService"
            android:exported="true"
            android:label="Nuova task"
            android:permission="com.google.android.wearable.permission.BIND_TILE_PROVIDER">
            <intent-filter>
                <action android:name="androidx.wear.tiles.action.BIND_TILE_PROVIDER" />
            </intent-filter>
            <meta-data
                android:name="androidx.wear.tiles.PREVIEW"
                android:resource="@drawable/tile_preview_add_task" />
        </service>
    </application>
</manifest>
